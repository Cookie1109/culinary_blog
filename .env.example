# Development-only values. Copy to .env and change them for shared environments.
POSTGRES_DB=culinary_blog
POSTGRES_USER=culinary
POSTGRES_PASSWORD=local-development-only
MINIO_ROOT_USER=culinary-local
MINIO_ROOT_PASSWORD=local-development-only
MINIO_BUCKET=culinary-media
SEQ_ADMIN_PASSWORD=
NEXT_PUBLIC_API_BASE_URL=/api/v1
JWT_SIGNING_KEY=replace-with-at-least-32-random-characters
ADMIN_SEED_ENABLED=false
ADMIN_EMAIL=
ADMIN_PASSWORD=
